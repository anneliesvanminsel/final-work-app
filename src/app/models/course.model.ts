export class Course {
    public id: string;
    public name: string;
    public date: string;

    constructor (id: string, name: string, date: string) {
        this.id = id;
        this.name = name;
        this.date = date;
  }
}
